#
# Makefile.am for yast2/library/cron/src
#

agent_SCRIPTS = \
	ag_cron

scrconf_DATA = $(wildcard *.scr)

module_DATA = $(wildcard *.ycp) 

EXTRA_DIST = $(scrconf_DATA)  $(agent_SCRIPTS) $(module_DATA)



include $(top_srcdir)/Makefile.am.common
