
# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?)
#

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (CYAN,BLUE,OFF)

# Shadow color
shadow_color = (BLACK,BLACK,OFF)

# Dialog box color
dialog_color = (WHITE,BLUE,ON)

## yast title (yast at hostname)
# Dialog box title color
title_color = (BLACK,CYAN,OFF)

# Dialog box border color
border_color = (WHITE,WHITE,ON)

# Active button color
button_active_color = (BLACK,CYAN,OFF)

# Inactive button color
button_inactive_color = (WHITE,BLUE,OFF)

# Active button key color
button_key_active_color = (YELLOW,CYAN,ON)

# Inactive button key color
button_key_inactive_color = (YELLOW,BLUE,ON)

# Active button label color
button_label_active_color = (BLACK,CYAN,OFF)

# Inactive button label color
button_label_inactive_color = (WHITE,BLUE,OFF)

## yast-inactive-frame
# Input box color
inputbox_color = (WHITE,BLUE,OFF)

## used for yast-active-frame
# Input box border color
inputbox_border_color = (BLUE,BLUE,ON)

# Search box color
searchbox_color = (BLACK,WHITE,OFF)

# Search box title color
searchbox_title_color = (YELLOW,WHITE,ON)

# Search box border color
searchbox_border_color = (WHITE,WHITE,ON)

# File position indicator color
position_indicator_color = (YELLOW,WHITE,ON)

# Menu box color
menubox_color = (WHITE,BLUE,ON)

# Menu box border color
menubox_border_color = (WHITE,BLUE,OFF)

# Item color
item_color = (WHITE,BLUE,OFF)

# Selected item color
item_selected_color = (YELLOW,CYAN,ON)

# Tag color
tag_color = (WHITE,BLUE,OFF)

# Selected tag color
tag_selected_color = (YELLOW,CYAN,ON)

# Tag key color
tag_key_color = (WHITE,BLUE,OFF)

# Selected tag key color
tag_key_selected_color = (YELLOW,CYAN,ON)

## yast head-line (yast control center)
# Check box color
check_color = (YELLOW,BLUE,ON)

# Selected check box color
check_selected_color = (WHITE,BLUE,ON)

# Up arrow color
uarrow_color = (WHITE,BLUE,ON)

# Down arrow color
darrow_color = (WHITE,BLUE,ON)
