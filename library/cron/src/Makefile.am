# Sources for yast2

module_DATA = \
  modules/Cron.rb

scrconf_DATA = \
  scrconf/cron.scr

agent_SCRIPTS = \
  servers_non_y2/ag_cron

EXTRA_DIST = $(module_DATA) $(scrconf_DATA) $(agent_SCRIPTS)

include $(top_srcdir)/Makefile.am.common