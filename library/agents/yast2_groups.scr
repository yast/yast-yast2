/**
 * File:	yast2_groups.scr
 * Summary:	Agent for yast2 groups (y2cc groups)
 * Author:	Michal Svec <msvec@suse.cz>
 * Access:	read only
 *
 * Example:
 *   Dir(.yast2.groups)
 *   (["lan", "modem", "isdn", ...])
 **
 *   Dir(.yast2.groups.lan)
 *   (["Name", "Group", ...])
 **
 *   Read(.yast2.groups.lan.Textdomain)
 *   ("network")
 *
 * $Id$
 */
.yast2.groups

`ag_ini(
    `IniAgent( "/usr/share/YaST2/config/y2cc.groups",
	$[
	    "options"	: [ "read_only" ],
	    "comments"	: [ "^[ \t]*;.*", ";.*", "\\{[^}]*\\}", "^[ \t]*$" ],
	    "sections"	: [
		$[ "begin" : [
		    "^[ \t]*\\[[ \t]*Y2Group[ \t]*(.*[^ \t])[ \t]*\\][ \t]*",
		    "[Y2Group %s]",
		]],
	    ],
	    "params" : [
		$[ "match" : [
		    "^[ \t]*([^ ]*)[ \t]*=[ \t]*(.*)[ \t]*$",
		    "%s = %s",
		]],
	    ],
	]
    )
)
